// Массив с названиями фильтов
const captions = [
  `all`,
  `overdue`,
  `today`,
  `favorites`,
  `repeating`,
  `tags`,
  `archive`
];

const mockFiltersData = {
  captions
};

export default mockFiltersData;
